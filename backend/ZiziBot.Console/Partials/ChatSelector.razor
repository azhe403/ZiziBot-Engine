@inherits ReactiveInjectableComponentBase<ChatSelectorViewModel>

<RadzenText>Pilih Obrolan:</RadzenText>
<RadzenListBox @bind-Value="SelectedChatId" Change="OnValueChanged" Data="ListChat" LoadData="OnLoadData" Style="width: 100%; height: calc(100% - 32px);" TextProperty="ChatTitle" TValue="long" ValueProperty="ChatId">
    <Template>
        @((context as ChatInfoDto).ChatTitle)
    </Template>
</RadzenListBox>